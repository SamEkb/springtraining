spring:
  test:
    database:
      replace: none
  datasource:
    url: jdbc:tc:postgresql:14-alpine:///testdb"
  liquibase:
    enabled: true
    change-log: classpath:db.changelog/db.changelog-test.yml
  shell:
    interactive:
      enabled: false